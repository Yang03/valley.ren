
var width = window.innerWidth
var SW = new SiriWave({
    width: 450,
    height: 100,
    speed: 0.03,
    amplitude: 1,
    container: document.getElementById('siric'),
    autostart: true,
    color : '#000',
    style: 'ios9',
});
